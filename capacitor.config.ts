import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'catalago.de.produtos',
  appName: 'catalago-de-produtos',
  webDir: 'www'
};

export default config;
